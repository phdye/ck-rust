0. On the github website, branch phdye was updated just a moment ago.

1. git fetch --all && git merge origin/phdye

2. design-base = 'phdye', execute phdye/create-tests-from-design.md
   - Implement ALL modules from phdye/design/implementation-order.md (Tiers 0-5)
   - Commit and push after EACH module

3. design-base = 'phdye', execute phdye/implement-from-design.md
   - Implement ALL modules from phdye/design/implementation-order.md (Tiers 0-5)
   - Commit and push after EACH module
